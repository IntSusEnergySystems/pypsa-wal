# Downloaded GIS source data and derived rasters are reproducible: only the
# code, the config and the small summary tables are versioned.
data/
resources/
logs/
.snakemake/
*.log
results/figures/
results/potential/
results/profiles/
results/*.tif
results/*.gpkg

# The infographic is rebuilt from the tables above.
results/infographic/
